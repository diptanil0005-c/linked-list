#include <stdio.h>
int asum(int arr[],int n){
    if(n<=0){
        return 0;
    }
else{
    return arr[n-1]+asum(arr,n-1);
}
}
int main()
{
    int arr[5]={1,2,3,4,5};
   
    int size= sizeof(arr)/sizeof(arr[0]);
    int sum=asum(arr,size);
    printf("%d",sum);

    return 0;
}